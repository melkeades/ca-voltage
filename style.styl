// div:has(~ .products-sec) { display: none; }

[data-video-urls] video { display: none; }

.navbar__dd__list
  // transition all 300ms !important

[slide-cut] .w-dyn-item
  display flex !important
  //display block !important

.splide__list.w-dyn-items
  justify-content start
  grid-row-gap unset
  grid-column-gap unset

.tests__slider
  .splide__track
    overflow visible !important

html.lenis
  height auto

.lenis.lenis-smooth
  scroll-behavior auto

.lenis.lenis-smooth [data-lenis-prevent]
  overscroll-behavior contain

.lenis.lenis-stopped
  overflow hidden

.lenis.lenis-scrolling iframe
  pointer-events none

.map__h__num
  display inline-flex
  overflow hidden 
.map__num
  display inline-flex
  width .625em 
  height 1.2em